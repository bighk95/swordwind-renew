import './App.css';
import React from 'react';
import Layout from './Layout';
function App() {
  return (
    <div>
      <Layout />
    </div>
  );
}

/* 
  1. 핸디캡 / 어드밴티지 구현 (새로고침) [?]
  2. 전적 갱신 버튼 구현해야함. [?]
  3. UI 업그레이드 [진행중]
  4. 더보기 (API 연결한 후 작업할것.) [?]
  5. 먹꾸 어케하노? (Fetch loading) [?]
  6. 디테일 아이템 추가 [?]
*/

export default App;
